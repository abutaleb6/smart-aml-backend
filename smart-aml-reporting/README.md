# Smart AML - Reporting Module

This module provides GoAML report creation and listing APIs. It persists goaml_reports table rows and exposes simple endpoints to create and retrieve reports.

Migrations
- The Flyway migration for goaml_reports (V10) exists in smart-aml-reporting/src/main/resources/db/migration/ in the repository root modules (ensure it is present and applied).
